import * as React from 'react';
import { IStackProps } from './IStackProps';
export declare const useStackStyles: (props: IStackProps) => {
    stackStyles: (containerWidth: number, containerHeight: number) => React.CSSProperties;
};
