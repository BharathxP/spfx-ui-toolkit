import * as React from 'react';
import { ICardProps } from './ICardProps';
export declare const useCardStyles: (props: ICardProps) => {
    cardStyles: (containerWidth: number, containerHeight: number) => React.CSSProperties;
    bodyCardDefaultStyles: React.CSSProperties;
};
