import * as React from 'react';
interface GenericOverFlowMenuProps<T> {
    items: T[];
    renderItem: (item: T) => React.ReactNode;
    keyExtractor: (item: T) => string;
}
export declare const GenericOverFlowMenu: <T extends {
    id: string;
}>({ items, renderItem, keyExtractor, }: GenericOverFlowMenuProps<T>) => JSX.Element | null;
export {};
