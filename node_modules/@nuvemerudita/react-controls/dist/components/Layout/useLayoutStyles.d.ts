import * as React from "react";
import { ILayoutConfig, ILayoutProps } from "./ILayoutProps";
export declare const useLayoutStyles: (props: ILayoutProps & {
    layoutConfig?: ILayoutConfig;
}) => {
    rootStyles: (LayoutWidth: number, LayoutHeight: number) => React.CSSProperties;
    layoutGridStyles: string;
};
