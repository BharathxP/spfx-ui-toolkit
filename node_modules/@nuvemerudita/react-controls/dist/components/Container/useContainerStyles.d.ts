import * as React from 'react';
import { IContainerProps } from './IContainerProps';
export declare const useContainerStyles: (props: IContainerProps) => {
    containerStyles: (containerWidth: number, containerHeight: number) => React.CSSProperties;
};
