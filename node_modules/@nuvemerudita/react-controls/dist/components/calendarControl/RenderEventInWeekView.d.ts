import * as React from "react";
export interface IRenderEventInWeekViewProps {
    events: {
        title: string;
        start: string;
        end: string;
        participants?: string[];
    }[];
    date: Date;
    columnIndex: number;
}
export declare const RenderEventInWeekView: React.FunctionComponent<IRenderEventInWeekViewProps>;
