import { IEvent } from "./IEvents";
export interface TimeSlot {
    time: string;
    events: IEvent[];
}
export interface DayEvents {
    date: string;
    fullDayEvents: IEvent[];
    timeSlots: TimeSlot[];
}
export interface IUseCalendar {
    getMonthCalendar: (year: number, month: number) => Record<string, IEvent[]>;
    addEvent: (event: IEvent) => void;
    getWeekEvents: (startDate: string) => DayEvents[];
}
export declare const useCalendar: (initialEvents: IEvent[], timezone: string) => IUseCalendar;
export default useCalendar;
