import * as React from 'react';
import { ICarouselProps } from './ICarouselProps';
export declare const useCarouselStylesMain: (props: ICarouselProps) => {
    carouselStyles: (containerWidth: number, containerHeight: number) => React.CSSProperties;
};
