import * as React from 'react';
import { ICenterProps } from './ICenterProps';
export declare const useCenterStyles: (props: ICenterProps) => {
    centerStyles: (containerWidth: number, containerHeight: number) => React.CSSProperties;
    childrenStyles: string;
};
