import * as React from 'react';
import { ISpaceProps } from './ISpaceProps';
export declare const useSpaceStyles: (props: ISpaceProps) => {
    spaceStyles: (containerWidth: number) => React.CSSProperties;
};
