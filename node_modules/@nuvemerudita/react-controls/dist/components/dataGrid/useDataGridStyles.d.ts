import { IDataGridProps } from './IDataGridProps';
export declare const useDataGridStyles: <T>(props: IDataGridProps<T>) => {
    dataGridStyles: (containerWidth: number, containerHeight: number) => {
        datagridStyles: string;
        rowSelection: string;
    };
};
